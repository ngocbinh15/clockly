import 'package:flutter/material.dart';

class PageCalendar extends StatelessWidget {
  const PageCalendar({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Page calendar"),
      ),
    );
  }
}
